// Membuat variabel array menggunakan const
const animals = ['Harimau', 'Singa', 'Gajah', 'Kuda', 'Kucing'];

// looping array  menggunakan for-of
for (const animal of animals) {
    console.log(`Hewan: ${animal}`);
}
